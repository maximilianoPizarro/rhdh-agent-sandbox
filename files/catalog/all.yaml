apiVersion: backstage.io/v1alpha1
kind: Location
metadata:
  name: rhdh-agent-sandbox-catalog
  description: AI catalog locations for agent-friendly Developer Hub demo
spec:
  targets:
    # Paths relative to this file (ConfigMap mount flattens siblings).
    - ./system.yaml
    - ./locations.yaml
    - ./mcp-servers.yaml
    - ./skills.yaml
    - ./prompts.yaml
    - ./users.yaml
    - ./template-devspaces-workspace.yaml
    - ./template-ai-service.yaml
    - ./template-deploy-agent.yaml
