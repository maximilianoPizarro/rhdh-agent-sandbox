apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: ${{ values.name | default "quarkus-agent" }}
  annotations:
    rhdh-agent-sandbox.io/managed-agent: "true"
    rhdh-agent-sandbox.io/language: quarkus
    rhdh-agent-sandbox.io/framework: langchain4j
spec:
  type: service
  lifecycle: experimental
  owner: ${{ values.owner | default "group:default/developers" }}
