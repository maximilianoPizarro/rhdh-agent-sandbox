apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: ${{ values.name }}
  description: AI service wired for LiteLLM model ${{ values.model }}
  tags: [ai, mcp, lightspeed]
spec:
  type: service
  lifecycle: experimental
  owner: ${{ values.owner }}
  dependsOn:
    - api:default/openshift-mcp-server
    - api:default/rhdh-mcp-actions
