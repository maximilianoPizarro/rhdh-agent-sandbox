# ${{ values.name }}

${{ values.description }}

Stack: `${{ values.stack }}`

## DevSpaces + Cursor

1. Open this repo in OpenShift DevSpaces (Devfile included).
2. Connect Cursor with Remote SSH to the workspace.
3. Install Hub skills (`skill-devspaces-cursor`, `skill-openshift-mcp`).
