# ${{ values.name }}

Quarkus / **LangChain4j** agent generated by the Deploy Agent golden path.

## What was generated

- `@RegisterAiService` chat interface with tools
- REST endpoints: `/health`, `/`, `/v1/chat`, `/v1/runtime`
- Tools: `listPods`, `lookupCve`, `checkLifecycle` (for tool-agent demos)
- LiteLLM via `quarkus.langchain4j.openai.*`

## Build & run on OpenShift

Quarkus Maven builds need more CPU/memory (see BuildConfig limits).

```bash
oc get bc/${{ values.name }}
oc logs -f bc/${{ values.name }}
oc get deploy/${{ values.name }}
```

## Red Hat agentic skills

https://www.redhat.com/en/agentic-skills — CVE / lifecycle tools are also wired in `AgentTools`.
