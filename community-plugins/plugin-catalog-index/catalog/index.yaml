apiVersion: backstage.io/v1alpha1
kind: Location
metadata:
  name: rhdh-agent-sandbox-ai-index
  description: AI assets index for Extensions / catalog discovery
spec:
  targets:
    - ./ai-assets.yaml
